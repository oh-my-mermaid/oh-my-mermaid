The server is the only component that holds Gemini API keys and Supabase service role credentials. The generation pipeline has two generator variants: generator.py (Gemini) and generator_comfy.py (ComfyUI) — the ComfyUI path appears to be an incomplete alternative. The pipeline/templates.py and pipeline/translator.py suggest a prompt templating system for image generation requests. Alembic is in dev dependencies meaning migrations are a developer operation, not a runtime one.
