These scripts represent the content pipeline for the agito office world's built-in asset library. The two splitter variants (careful vs floodfill) suggest iterative refinement of the sprite sheet splitting approach. The cyan chroma approach (generate on #00FF00 green background then remove) is a classic game asset pipeline technique. Generated assets end up in agito-app/builtin-assets/ and are bundled into the Electron binary.
